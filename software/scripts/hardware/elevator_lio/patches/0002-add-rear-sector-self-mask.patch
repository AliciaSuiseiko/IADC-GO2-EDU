diff --git a/include/support/common_lib.h b/include/support/common_lib.h
index f106886..d84dc4f 100644
--- a/include/support/common_lib.h
+++ b/include/support/common_lib.h
@@ -30,6 +30,7 @@
 #include <chrono>      // std::chrono::milliseconds
 #include <algorithm>
 #include <cctype>
+#include <cmath>
 #include <unordered_map>
 #include <unordered_set>
 #include <iomanip>     // std::setw
@@ -392,6 +393,9 @@ extern bool use_box_blind; // true: 删除 LiDAR 坐标系中指定长方体内
 extern Eigen::Vector2d box_corner_x; // 长方体 x 轴范围 [min, max]
 extern Eigen::Vector2d box_corner_y; // 长方体 y 轴范围 [min, max]
 extern Eigen::Vector2d box_corner_z; // 长方体 z 轴范围 [min, max]
+extern bool use_rear_sector_blind; // 删除 Go2 body -X 方向附近的近场自机回波
+extern double rear_sector_blind_range; // 后方扇锥的三维距离上限 [m]
+extern double rear_sector_blind_half_angle_deg; // 后方扇锥的水平半角 [deg]
 extern float filter_size; // 点云降采样大小（ikdtree大小）
 extern bool downsample_adaptive_enabled; // 是否启用变体素大小降采样
 extern double downsample_target_points; // Adaptive voxel target points per second
@@ -401,8 +405,9 @@ extern double downsample_max_voxel;     // Adaptive voxel max
 extern double local_map_cube_len;       // Local ikd-tree map cube edge length in metres
 
 // 根据当前盲区配置判断 LiDAR 原始点是否保留。
-// box 模式会删除长方体内（含边界）的自遮挡点；其他模式删除 blind 半径内的点。
+// 基础盲区后可叠加以 Go2 body -X 方向为中心的近场后方扇锥。
 inline bool keepLidarPoint(float x, float y, float z) {
+    bool keep = true;
     if (use_box_blind) {
         const double min_x = std::min(box_corner_x[0], box_corner_x[1]);
         const double max_x = std::max(box_corner_x[0], box_corner_x[1]);
@@ -414,9 +419,27 @@ inline bool keepLidarPoint(float x, float y, float z) {
             x >= min_x && x <= max_x &&
             y >= min_y && y <= max_y &&
             z >= min_z && z <= max_z;
-        return !inside_box;
+        keep = !inside_box;
+    } else {
+        keep = x * x + y * y + z * z > blind * blind;
     }
-    return x * x + y * y + z * z > blind * blind;
+    if (!keep || !use_rear_sector_blind) return keep;
+
+    const double range_sq = x * x + y * y + z * z;
+    const double max_range_sq = rear_sector_blind_range * rear_sector_blind_range;
+    if (range_sq > max_range_sq) return true;
+
+    // Rotate the ray direction into the calibrated Go2 body orientation.
+    const double body_x = lidar_R_body(0, 0) * x + lidar_R_body(1, 0) * y +
+                          lidar_R_body(2, 0) * z;
+    const double body_y = lidar_R_body(0, 1) * x + lidar_R_body(1, 1) * y +
+                          lidar_R_body(2, 1) * z;
+    if (body_x >= 0.0) return true;
+
+    const double pi = std::acos(-1.0);
+    const double half_angle_rad = rear_sector_blind_half_angle_deg * pi / 180.0;
+    const double rear_angle = std::abs(std::atan2(body_y, -body_x));
+    return rear_angle > half_angle_rad;
 }
 
 extern bool global_map_save_enable; // 是否启用点云保存
diff --git a/src/support/common_lib.cpp b/src/support/common_lib.cpp
index bc4284c..bbef446 100644
--- a/src/support/common_lib.cpp
+++ b/src/support/common_lib.cpp
@@ -204,6 +204,9 @@ bool use_box_blind = false;
 Eigen::Vector2d box_corner_x(-0.7, 0.1);
 Eigen::Vector2d box_corner_y(-0.3, 0.3);
 Eigen::Vector2d box_corner_z(-0.4, 0.4);
+bool use_rear_sector_blind = false;
+double rear_sector_blind_range = 0.5;
+double rear_sector_blind_half_angle_deg = 10.0;
 float filter_size = 0.5; // 点云降采样大小（ikdtree大小），默认值0.5
 bool downsample_adaptive_enabled = false; // 是否启用变体素大小降采样
 double downsample_target_points = 2500.0; // 自适应体素的目标每秒点数
@@ -369,6 +372,24 @@ void load_lio_yaml(const std::vector<string>& file_names) {
         } else if (use_box_blind) {
             ConfigPrinter::printError("box_corner.box_corner_z");
         }
+        if (reader.read("downsample.rear_sector_blind.enable", use_rear_sector_blind)) {
+            ConfigPrinter::printItemBool("rear_sector_blind.enable", use_rear_sector_blind);
+        } else {
+            ConfigPrinter::printItemBool("rear_sector_blind.enable", use_rear_sector_blind, true);
+        }
+        if (reader.read("downsample.rear_sector_blind.range", rear_sector_blind_range)) {
+            ConfigPrinter::printItem("rear_sector_blind.range", rear_sector_blind_range);
+        } else if (use_rear_sector_blind) {
+            ConfigPrinter::printError("rear_sector_blind.range");
+        }
+        if (reader.read("downsample.rear_sector_blind.half_angle_deg", rear_sector_blind_half_angle_deg)) {
+            ConfigPrinter::printItem("rear_sector_blind.half_angle_deg", rear_sector_blind_half_angle_deg);
+        } else if (use_rear_sector_blind) {
+            ConfigPrinter::printError("rear_sector_blind.half_angle_deg");
+        }
+        rear_sector_blind_range = std::max(0.0, rear_sector_blind_range);
+        rear_sector_blind_half_angle_deg =
+            std::clamp(rear_sector_blind_half_angle_deg, 0.0, 180.0);
         if (reader.read("downsample.filter_size", filter_size)) {
             ConfigPrinter::printItem("filter_size", filter_size);
         } else {
